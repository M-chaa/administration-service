apiVersion: v1
kind: Service
metadata:
  name: administration-service
spec:
  type: NodePort
  selector:
    app: administration-service
  ports:
    - port: 8088
      targetPort: 8088
      nodePort: 30088
